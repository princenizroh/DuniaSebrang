using UnityEngine;
// using Cinemachine;

public class CameraManager : MonoBehaviour
{
//     public CinemachineVirtualCamera[] cameras;
//     public CinemachineVirtualCamera startCamera;
//     private CinemachineVirtualCamera currentCam;

    private void Start()
    {
//         // Set kamera awal
//         currentCam = startCamera;

//         for (int i = 0; i < cameras.Length; i++)
//         {
//             cameras[i].Priority = (cameras[i] == currentCam) ? 20 : 10;
//         }
    }

//     public void SwitchCamera(int cameraIndex)
//     {
//         // Validasi indeks kamera
//         if (cameraIndex < 0 || cameraIndex >= cameras.Length)
//         {
//             Debug.LogError($"Kamera dengan indeks {cameraIndex} tidak valid!");
//             return;
//         }

//         // Pindahkan ke kamera baru
//         currentCam = cameras[cameraIndex];
//         for (int i = 0; i < cameras.Length; i++)
//         {
//             cameras[i].Priority = (i == cameraIndex) ? 20 : 10;
//         }
//     }
}
